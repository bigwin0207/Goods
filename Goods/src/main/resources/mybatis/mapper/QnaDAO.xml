<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.goods.g1.dao.IQnaDAO">
    <select id="getAllQna" resultType="com.goods.g1.dto.QnaVO">
        select * from Qna
    </select>

    <select id="getQna" resultType="com.goods.g1.dto.QnaVO">
        select * from qna where qseq=#{param1}
    </select>
</mapper>